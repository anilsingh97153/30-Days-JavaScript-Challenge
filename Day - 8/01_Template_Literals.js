// Use of template literals to create a string that include variables.

let myName = "Anil Singh";
let age = 22;
let result = `${myName} - ${age}`;
console.log(result);

// Multi-line string using template literals

let multiLineString = `This is a string
that spans multiple
lines`;

console.log(multiLineString);
